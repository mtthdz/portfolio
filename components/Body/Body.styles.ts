import styled from "styled-components";

export const SectionStyles = styled.section`
  h1 {
    font-size: 1.4rem;
    font-weight: 300;
    margin-bottom: 20px;
  }

  p {
    font-size: 1.4rem;
    margin-bottom: 12px;
  }
`